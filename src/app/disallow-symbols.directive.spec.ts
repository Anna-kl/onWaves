import { DisallowSymbolsDirective } from './disallow-symbols.directive';

describe('DisallowSymbolsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisallowSymbolsDirective();
    expect(directive).toBeTruthy();
  });
});
